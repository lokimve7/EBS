# Pending Issues

## 목적

이 문서는 아직 해소되지 않은 구조 의심과 보류 이슈를 기록하고 추적하기 위한 문서입니다.

## 보류된 의심
- `public/index.html`, `public/styles/main.css`, `public/scripts/main.js`: 탭별 상세 화면, 스크립트 탭 전용 입력 동작, DB 프로젝트 목록 선택 모달, 공통 API 키 설정 모달, 프로젝트 고유 ID 상태 전달, DB 결과 URL 조회와 카드 상태 병합 흐름이 같은 파일에 함께 쌓이고 있다. 남은 탭 작업이 2개 이상 진행되기 전 또는 공통 모달이 하나 더 추가되기 전에 `탭 공통 영역`과 `탭별 영역`의 마크업/스타일/스크립트 분리 task를 별도로 검토한다.
- `server.js`: 정적 파일 제공, OpenAI 기반 스크립트 생성 API 중계, EvoLink.AI 영상 생성 API 중계, 프로젝트 고유 ID 기반 파일 저장 흐름, DB 프로젝트 목록/상세 API와 이미지/영상 결과 메타데이터 API 연결이 한 파일에 함께 들어 있다. DB 책임은 `database.js`로 분리했지만, 서버 기능이 더 늘어나기 전 정적 파일 응답, API 요청 처리, 프로젝트 파일 저장 유틸 책임 분리 task를 별도로 검토한다.
