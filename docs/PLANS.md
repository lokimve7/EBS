# Plans

## 목적

이 문서는 현재 진행 중인 단계 상태와 최근 작업 흐름을 빠르게 확인하기 위한 기록 문서입니다.

## 작업리스트
| 단계 | Task 문서 | 해야 할 항목 | 상태 |
| --- | --- | --- | --- |
| 01-1 | [Tasks/01-1.md](../Tasks/01-1.md) | `localhost:3001`에서 접속 가능한 기본 서버를 실행하고, 접속 시 `환영합니다` 문구와 서버 실행 안내 문구를 확인할 수 있게 한다. | 완료 |
| 01-2 | [Tasks/01-2.md](../Tasks/01-2.md) | `Layout/layout_top.png`와 최대한 비슷한 공통 상단 영역과 탭 메뉴가 있는 메인 화면을 만들고, 탭 클릭 시 상단 아래의 내용 영역만 전환되게 한다. | 완료 |
| 01-3 | [Tasks/01-3.md](../Tasks/01-3.md) | `Layout/01_layout_script.png`를 참고해 스크립트 탭의 하단 내용 영역을 `스크립트 설정`, `장면 구성` 화면으로 구성한다. | 완료 |
| 01-4 | [Tasks/01-4.md](../Tasks/01-4.md) | 스크립트 탭의 `스크립트 설정` 영역에서 주제, 분위기, 스타일, 장면 수를 직접 입력 또는 선택할 수 있게 하고, `스크립트 생성` 버튼 클릭 시 진행 중 상태가 보이게 한다. | 완료 |
| 01-5 | [Tasks/01-5.md](../Tasks/01-5.md) | `Layout/layout_api_key.png`를 참고해 상단의 `API 키 설정` 버튼을 누르면 공통 API 키 설정 창이 열리고, 저장 및 취소 동작과 저장된 키 유지 결과를 확인할 수 있게 한다. | 완료 |
| 01-6 | [Tasks/01-6.md](../Tasks/01-6.md) | OpenAI API 공식문서를 기준으로 `스크립트 생성` 버튼 클릭 시 입력한 주제, 분위기, 스타일, 장면 수를 바탕으로 장면별 스크립트 데이터를 받아오고, 응답 개수만큼 씬 카드를 생성해 화면에 표시한다. | 완료 |
| 01-7 | [Tasks/01-7.md](../Tasks/01-7.md) | `Layout/02_layout_image.png`를 참고해 이미지 탭의 하단 내용 영역을 장면별 이미지 카드 목록 화면으로 구성하고, 카드 목록은 한 화면 높이 안에서 가로 스크롤되게 한다. | 완료 |
| 01-8 | [Tasks/01-8.md](../Tasks/01-8.md) | 스크립트 생성이 완료되면 프로젝트별 결과물이 한 폴더에 모이도록 저장 구조를 만들고, 스크립트 결과를 자동 저장하며, 이후 이미지·영상·보이스 등을 따로 저장할 수 있는 하위 폴더 구조를 함께 준비한다. | 완료 |
| 01-9 | [Tasks/01-9.md](../Tasks/01-9.md) | 상단 `프로젝트 불러오기` 버튼을 눌렀을 때 저장된 프로젝트의 스크립트 파일을 선택해 불러올 수 있고, 선택한 파일의 장면 데이터로 스크립트 탭의 씬 카드들이 다시 구성되게 한다. | 완료 |


## 상태 범례

- `예정`: 아직 시작 전
- `진행중`: 구현 중이거나, 구현 후 사용자 수동 작업 또는 결과 확인을 기다리는 상태
- `완료`: 구현 및 현재 확인 모드 기준 확인 완료
- `차단됨`: 오류, 필수 정보 누락, 외부 의존성 문제 등으로 정상 진행이 막힌 상태

## 최근 작업 로그
- 2026-07-06: `01-8` 승인 후 `server.js`에 프로젝트 폴더 이름 정리, `projects/` 루트 생성, `scripts/images/videos/voices` 하위 폴더 보장, `scripts/scene-script.json` 자동 저장 로직을 추가했다.
- 2026-07-06: `Tasks/01-8.md`, `docs/ARCHITECTURE.md`, `docs/PENDING_ISSUES.md`를 실제 구현 기준으로 갱신했고 `node --check server.js` 정적 점검을 완료했다.
- 2026-07-04: `01-7`을 `진행중`으로 전환하고 `이미지` 탭 패널의 현재 비어 있는 구조와 기존 탭 레이아웃 파일 책임을 점검했다.
- 2026-07-04: `public/index.html`, `public/styles/main.css`에 `Layout/02_layout_image.png` 기준의 이미지 씬 카드 레이아웃과 가로 스크롤 카드 영역을 구현하고 `node --check public/scripts/main.js` 정적 점검을 완료했다.
- 2026-07-04: `Tasks/01-7.md`, `docs/ARCHITECTURE.md`를 실제 구현 기준으로 갱신했다.
- 2026-06-30: `01-6`를 `진행중`으로 전환하고 OpenAI API 요청 경로, 화면 상태 갱신 지점, 구조 의심 파일을 검토했다.
- 2026-06-30: `server.js`, `public/index.html`, `public/scripts/main.js`에 OpenAI Responses API 기반 장면 스크립트 생성 요청, 응답 스키마 처리, 동적 씬 카드 렌더링을 구현했다.
- 2026-06-30: `node --check server.js`, `node --check public/scripts/main.js` 정적 문법 점검을 완료했고, `public/styles/main.css`에 생성 상태 배너 색상 분기를 추가했다.
- 2026-06-30: `Tasks/01-6.md`, `docs/ARCHITECTURE.md`, `docs/PENDING_ISSUES.md`를 실제 구현 기준으로 갱신했고, 오래된 최근 작업 로그를 `docs/LOGS.md`로 이동했다.
- 2026-06-30: 서버 터미널에서 스크립트 생성 요청 흐름을 확인할 수 있도록 `server.js`에 요청 수신, OpenAI 호출, 응답 성공/실패 로그를 추가했다.

